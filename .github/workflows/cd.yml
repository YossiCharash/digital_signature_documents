# CD – פריסה ל-Render (Deploy Hook). ראו docs/DEPLOY_RENDER.md.
name: CD (Render)

on:
  workflow_run:
    workflows: ["CI"]
    branches: [main]
    types:
      - completed
  workflow_dispatch:

jobs:
  # ============================================
  # Trigger Render Deploy
  # ============================================
  deploy:
    name: Deploy to Render
    runs-on: ubuntu-latest
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event.workflow_run.conclusion == 'success')
    steps:
      - name: Trigger Render deploy
        run: |
          if [ -z "${{ secrets.RENDER_DEPLOY_HOOK_URL }}" ]; then
            echo "::warning::RENDER_DEPLOY_HOOK_URL not set. Add it in repo Settings → Secrets and variables → Actions."
            exit 0
          fi
          echo "Triggering Render deploy..."
          resp=$(curl -s -w "\n%{http_code}" -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}")
          http_code=$(echo "$resp" | tail -n1)
          body=$(echo "$resp" | sed '$d')
          echo "Response ($http_code): $body"
          if [ "$http_code" != "200" ]; then
            echo "::error::Render deploy hook returned HTTP $http_code"
            exit 1
          fi
          echo "Render deploy triggered successfully."

      - name: Deployment summary
        run: |
          echo "## 🚀 CD (Render)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Stage | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Deploy | ✅ Triggered |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Render will build and deploy from \`main\`. Check the [Render Dashboard](https://dashboard.render.com) for build status." >> $GITHUB_STEP_SUMMARY
